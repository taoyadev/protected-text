/**
 * Русские переводы - Основной файл экспорта
 *
 * Этот файл объединяет все строки русского перевода для приложения Protected Text.
 */

import { common } from './common';
import { landing } from './landing';
import { editor } from './editor';
import { dialogs } from './dialogs';
import { errors } from './errors';
import { toasts } from './toasts';
import { pro } from './pro';
import { metadata } from './metadata';

export const ru = {
  common,
  landing,
  editor,
  dialogs,
  errors,
  toasts,
  pro,
  metadata,
};

export default ru;
