/**
 * Traduções em português - Arquivo de exportação principal
 *
 * Este arquivo agrega todas as strings de tradução em português para a aplicação Protected Text.
 */

import { common } from './common';
import { landing } from './landing';
import { editor } from './editor';
import { dialogs } from './dialogs';
import { errors } from './errors';
import { toasts } from './toasts';
import { pro } from './pro';
import { metadata } from './metadata';

export const pt = {
  common,
  landing,
  editor,
  dialogs,
  errors,
  toasts,
  pro,
  metadata,
};

export default pt;
