/**
 * Traductions françaises - Fichier d'exportation principal
 *
 * Ce fichier agrège toutes les chaînes de traduction françaises pour l'application Protected Text.
 */

import { common } from './common';
import { landing } from './landing';
import { editor } from './editor';
import { dialogs } from './dialogs';
import { errors } from './errors';
import { toasts } from './toasts';
import { pro } from './pro';
import { metadata } from './metadata';

export const fr = {
  common,
  landing,
  editor,
  dialogs,
  errors,
  toasts,
  pro,
  metadata,
};

export default fr;
