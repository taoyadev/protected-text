/**
 * Deutsche Übersetzungen - Master-Exportdatei
 *
 * Diese Datei aggregiert alle deutschen Übersetzungszeichenfolgen für die Protected Text-Anwendung.
 */

import { common } from './common';
import { landing } from './landing';
import { editor } from './editor';
import { dialogs } from './dialogs';
import { errors } from './errors';
import { toasts } from './toasts';
import { pro } from './pro';
import { metadata } from './metadata';

export const de = {
  common,
  landing,
  editor,
  dialogs,
  errors,
  toasts,
  pro,
  metadata,
};

export default de;
