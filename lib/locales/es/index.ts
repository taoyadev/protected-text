/**
 * Traducciones al español - Archivo de exportación principal
 *
 * Este archivo agrega todas las cadenas de traducción al español para la aplicación Protected Text.
 */

import { common } from './common';
import { landing } from './landing';
import { editor } from './editor';
import { dialogs } from './dialogs';
import { errors } from './errors';
import { toasts } from './toasts';
import { pro } from './pro';
import { metadata } from './metadata';

export const es = {
  common,
  landing,
  editor,
  dialogs,
  errors,
  toasts,
  pro,
  metadata,
};

export default es;
