import { NextResponse } from 'next/server';

export async function POST() {
  // Placeholder for Stripe webhook handling.
  // Implement in Phase 3 once Stripe account + secrets are ready.
  return NextResponse.json({ ok: true });
}
